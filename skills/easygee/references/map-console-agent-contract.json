{
  "name": "EasyGEE Map Console Agent Contract",
  "version": 2,
  "stateEndpoint": "/api/session/state",
  "profileEndpoint": "/api/session/profile",
  "actionsEndpoint": "/api/session/actions",
  "capabilitiesEndpoint": "/api/session/capabilities",
  "layerEndpoint": "/api/layer",
  "uploadEndpoint": "/api/uploads",
  "analysisEndpoints": {
    "ndvi": "/api/analysis/ndvi"
  },
  "exportEndpoints": {
    "ndviDrive": "/api/export/ndvi-drive"
  },
  "planner": {
    "script": "scripts/resolve_ambiguous_geo_request.py",
    "agentCommand": "map_console_agent.py plan",
    "routes": ["gee_product", "gee_remote_sensing", "current_image_vision", "ask_user"],
    "purpose": "Resolve vague extraction requests such as water, rooftops, or visible-image objects before running analysis."
  },
  "stateFields": [
    "title",
    "project",
    "projectSource",
    "startDate",
    "endDate",
    "cloudPct",
    "center",
    "zoom",
    "bounds",
    "activeLayerId",
    "aoiBounds",
    "aoi",
    "hasExplicitAoi",
    "processingAoi",
    "processingBounds",
    "aoiShown",
    "aoiStyle",
    "measurements",
    "measurementsShown",
    "measurementsOpacity",
    "measurementSummary",
    "selectedDataset",
    "layers",
    "uploadCapabilities",
    "uploads",
    "tasks",
    "quota",
    "visualPreferences",
    "favoriteDatasets"
  ],
  "selectedDatasetFields": [
    "id",
    "label",
    "type",
    "source",
    "provider",
    "scale",
    "dateRange",
    "catalogUrl",
    "currentAoi",
    "currentBounds",
    "hasExplicitAoi",
    "processingAoi",
    "processingBounds",
    "mapDateRange",
    "defaultRecipe"
  ],
  "layerFields": [
    "id",
    "name",
    "dataset",
    "type",
    "shown",
    "opacity",
    "styleProfile",
    "stylePreset",
    "visParams",
    "legend",
    "recipe",
    "summary",
    "aoi"
  ],
  "recipe": {
    "purpose": "Describe how an Earth Engine layer was produced so agents can revise or reproduce it without guessing.",
    "kinds": ["preview", "imageCollection"],
    "imageCollectionFields": ["datasetId", "band", "outputBand", "temporalReducer", "year", "months", "startDate", "endDate", "scaleFactor", "offset", "visParams", "legend"],
    "temporalReducers": ["median", "mean", "max", "min", "mode", "mosaic"],
    "example": {
      "kind": "imageCollection",
      "datasetId": "MODIS/061/MOD13Q1",
      "band": "NDVI",
      "outputBand": "NDVI",
      "temporalReducer": "max",
      "year": 2024,
      "months": [5, 6, 7, 8, 9],
      "scaleFactor": 0.0001
    }
  },
  "profileFields": [
    "favoriteDatasets",
    "visualPreferences",
    "projects",
    "projects.<project>.tasks",
    "projects.<project>.uploads",
    "language",
    "updatedAt"
  ],
  "taskFields": [
    "id",
    "type",
    "analysis",
    "title",
    "status",
    "destination",
    "folder",
    "fileNamePrefix",
    "taskId",
    "taskName",
    "driveSearchUrl",
    "createdAt",
    "updatedAt",
    "params",
    "notes",
    "warnings"
  ],
  "uploads": {
    "endpoint": "/api/uploads",
    "formats": ["SHP", "ZIP", "KML", "KMZ", "GPX", "GeoJSON", "CSV", "GPKG"],
    "maxBytes": 52428800,
    "stateField": "uploads",
    "fields": ["id", "name", "storedName", "extension", "format", "size", "projection", "savedPath", "url", "componentPaths", "previewPath", "previewUrl", "previewFormat", "previewFeatureCount", "previewLimited", "previewError", "renderable", "agentReadable", "status", "createdAt", "updatedAt", "processingHints", "recommendedAgentAction", "layerId"],
    "behavior": "The browser uploads user-selected files to the local preview server under uploads/. Renderable uploads expose previewUrl/previewPath as GeoJSON and are added to the layer stack immediately. Non-renderable uploads, such as a standalone .dbf without .shp geometry, remain in state.uploads with savedPath/componentPaths and previewError, but are not added to the layer stack. Upload records can be removed from the workbench list; if a record has layerId, the corresponding map layer is removed too."
  },
  "aoi": {
    "defaultType": "rectangle",
    "types": ["rectangle", "polygon"],
    "coordinateOrder": "latlng"
  },
  "measurements": {
    "fields": ["id", "start", "end", "lengthMeters", "lengthLabel", "createdAt"],
    "summaryFields": ["count", "totalMeters", "meanMeters", "minMeters", "maxMeters"],
    "layerId": "__easygee_measurements__",
    "layerBehavior": "When measurements exist, the Map Console exposes them as a system layer in the layer stack. hide/show preserves saved measurements; remove clears them."
  },
  "actions": {
    "supported": ["addLayer", "removeLayer", "showLayer", "hideLayer", "selectLayer", "refreshLayer", "setLayerVisibility", "setLayerOpacity", "removeUpload", "selectDataset", "setAoi", "clearAoi", "setAoiStyle", "updateLayerStyle", "clearMeasurements", "extractNdvi", "addTask", "exportNdviDrive"],
    "resultSync": "Actions are executed by the browser console and reflected in the next session state sync."
  },
  "compactState": {
    "agentCommand": "map_console_agent.py state --compact",
    "purpose": "Return only the state fields agents usually need for follow-up work, omitting Earth Engine tile URLs and bulky AOI/layer detail. Includes a safe quota summary with status/source/usageSource so agents can detect default-only fallback state."
  },
  "quotaRefresh": {
    "agentCommand": "refresh_map_console_quota.py <map.html>",
    "purpose": "Refresh only the embedded STATE.quota object in an existing generated Map Console page after UI-only maintenance. The helper preserves layers, AOI, measurements, and tile URLs, and refuses to write default-only fallback quota state unless --allow-fallback is explicit."
  },
  "quickLayer": {
    "agentCommand": "map_console_agent.py quick-layer",
    "purpose": "Use deterministic fast paths for common layer requests before falling back to dataset search or explicit recipes.",
    "supportedFastPaths": ["sentinel2_ndvi", "modis_ndvi"]
  },
  "driveShortcut": {
    "elementId": "drive-btn",
    "purpose": "Left tool rail shortcut to Google Drive.",
    "targetRule": "Open the latest task.driveSearchUrl when available; otherwise open Google Drive My Drive."
  },
  "rules": [
    "Agents should use this contract or scripts/map_console_agent.py instead of reading generated HTML.",
    "Selected dataset context is exposed as selectedDataset; agents should use it to resolve deictic requests such as this MODIS dataset.",
    "ImageCollection analyses should use explicit recipe objects instead of relying on the default preview reducer.",
    "Layer recipe metadata must be preserved when restyling a layer so visualization changes do not alter the analytical calculation.",
    "Agents should use showLayer, hideLayer, selectLayer, refreshLayer, and setLayerOpacity for existing layer state changes instead of browser DOM interaction.",
    "Agents should call refreshLayer before treating a visible map layer as failed or missing, because the browser can recreate the Leaflet layer and bypass stale tile/vector requests.",
    "Agents should discover uploaded local files through state.uploads and state.uploadCapabilities, then process savedPath with local GIS tools instead of scraping file-picker DOM state.",
    "For common NDVI map requests, agents should try quick-layer first and fall back to render-recipe or dataset search only when no fast path matches.",
    "Persistent profile state is the durable source for dataset favorites, AOI, and measurements across localhost ports.",
    "AOI is represented as a system layer in the layer stack and can be styled, hidden, or cleared through the same layer workflow.",
    "Measurement markers are represented as a system layer in the layer stack and can be hidden, faded, or removed through the same layer workflow.",
    "For UI-only Map Console maintenance, do not regenerate a user-facing page with --no-live-quota or --no-quota-usage; patch the UI and use refresh_map_console_quota.py for quota-only refreshes. Non-sample pages require --allow-default-quota-state before create_map_console.py will write default-only quota state.",
    "When explicit AOI is cleared, aoi is null but processingAoi and processingBounds fall back to the current map viewport so preview layers can still be rendered.",
    "GEE layer visualization changes should update visParams through the workbench protocol and regenerate tile URLs instead of editing generated HTML.",
    "For vague extraction requests, run the planner first and ask one multiple-choice clarification when the route is ask_user.",
    "Do not add task-specific toolbar buttons; analysis results must sync back as normal map layers.",
    "Generated Earth Engine tile URLs are preview material and should not be copied into chat logs.",
    "Drive exports should sync a task record with taskId, folder, fileNamePrefix, and export parameters; agents should not require the user to know the final Drive file URL."
  ]
}
